apiVersion: api.cerbos.dev/v1
resourcePolicy:
  version: "default"
  resource: "uploads:object"
  rules:
    - actions: ['Upload']
      effect: EFFECT_ALLOW
      roles:
        - user
        - content_editor